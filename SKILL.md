---
name: wechat-h5-deck
description: "用代码生成微信端 H5 邀请函／营销画册（分屏吸附长页 ＋ 配套海报），替代稿定、易企秀这类在线 H5 编辑器。当用户提到做邀请函、活动详情页、H5 画册、营销长页、扫码看详情、微信推广页、发布会/工作坊/论坛的宣传物料、或说想脱离稿定/易企秀/MAKA 自己做时触发。产出是纯静态 HTML，可部署到 GitHub Pages、对象存储或自有服务器，改字即时生效、链接不变。"
---

# 微信 H5 邀请函与营销画册

把一次活动的信息，做成两件能直接发出去的东西：

- **一张海报**（1080×1920 PNG）——微信聊天里直接发图，扫码进详情页
- **一个详情页**（分屏吸附长页）——一屏一章，滑动翻页，可放导航、音乐、二维码

产物是纯静态 HTML。没有平台水印、没有"投诉"入口、链接是自己的域名，
改一个字重新上传就生效，已经转发出去的链接自动是新版。

## 什么时候用这个技能

适合：活动邀请函、课程/工作坊招募、发布会预告、论坛议程、招商画册、
项目汇报页 —— 凡是"发一张海报，扫码看详情"的场景。

不适合：需要用户填表提交、需要账号登录、需要复杂交互的页面。那些该做小程序或表单系统。

## 动线

```
海报（一张图，发微信群/朋友圈）
   → 扫码 → 详情页（7±2 屏，一屏一章）
   → 末屏行动点（导航 / 报名码 / 联系方式）
```

海报上的二维码**指向详情页，不要指向微信群**：群二维码只有 7 天有效期，
一旦印进海报就再也改不了；详情页地址永久有效，群码过期换张图重新上传即可。

## 工作流程

### 第一步：厘清内容结构

先问清楚这几件事，缺一件就不要动手排版：

| 必须有 | 说明 |
|---|---|
| 主办／承办／执行 | 决定署名行怎么写 |
| 活动全名 ＋ 副题 | 海报和封面的视觉主体 |
| 一句话主张 | 8–16 字，对参与者的承诺。写不出来说明活动定位还没想清楚 |
| 时间 ／ 地点 ／ 地址 | 地址要精确到能被地图搜到 |
| 日程 | 有几场、每场谁讲什么 |
| 行动点 | 扫码进群？报名表单？打电话？还是只看信息不转化 |

内容结构与每屏放什么，见 `references/04-内容与结构.md`。

### 第二步：改模板，不要从零生成

`templates/deck.html` 和 `templates/poster.html` 是成品，直接改里面的文字。
**不要重写 CSS** —— 那套排版系统（字阶、间距、纸感配色、名单对齐）是调过很多轮的，
从零生成大概率不如它。

改完把 `poster.html` 里的 `DETAIL_URL` 换成详情页的真实地址。

### 第三步：逐屏自检（这一步不能跳）

**硬约束：任何一屏都不许要下拉才能看全。** 陌生用户不知道下面还有内容，
被折在屏外的信息对他等于不存在。

把浏览器窗口调到 375×700，运行 `scripts/check-layout.js`，任何一屏报超出都要压缩。
超高最常见的原因是日程屏塞了太多场次 —— 超过 7 讲就拆成两屏。

### 第四步：出海报

```bash
./scripts/shot.sh        # 1080×1920，微信发图
./scripts/shot.sh 2      # 2160×3840，印刷备用
```

出图后**用二维码扫一次确认落点正确**。海报一旦发到群里就改不了了。

### 第五步：部署

三种路径，按需要选，见 `references/01-部署攻略.md`：

| 路径 | 成本 | 备案 | 适合 |
|---|---|---|---|
| GitHub Pages | 0 | 不需要 | 先看效果，国内访问偏慢 |
| 对象存储 ＋ 自有域名 | 几元/月 | **必须** | 国内微信正式传播 |
| 云服务器 ＋ Caddy | 几十元/月 | **必须** | 多项目复用、需要日志或接口 |

## 三条不能违反的规矩

1. **一屏一页** —— 见上文第三步。这是整个技能里最容易被忽略、又最影响实际效果的一条
2. **底部留白要算上安全区** —— iPhone 的 home indicator 会吃掉约 34px，
   `padding-bottom` 漏算这一段，底部内容就会被系统手势区盖住
3. **删元素必须同时删对应的 JS** —— 删了 HTML 留下 `getElementById` 的引用，
   脚本会在那一行抛 TypeError 并**整个中断**，后面所有功能静默失效。
   语法检查发现不了这种错

微信和 iOS 的完整踩坑清单见 `references/02-微信与iOS适配.md`，
包括自动播放背景音乐、翻页手感、字体加载、转发卡片。

## 参考资料

| 文件 | 内容 |
|---|---|
| `references/01-部署攻略.md` | 三层部署方案、域名备案、常见报错 |
| `references/02-微信与iOS适配.md` | 微信内核与 iOS 的坑，逐条给解法 |
| `references/03-设计系统.md` | 字阶、间距、配色、中文排版规则 |
| `references/04-内容与结构.md` | 每屏放什么、文案怎么写 |
| `PROMPT.md` | 给豆包／Kimi／通义等国内 AI 工具用的通用提示词 |
