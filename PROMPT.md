# 给对话框型 AI 用的提示词

如果你用的是 **WorkBuddy、豆包工作** 这类能操作本机的 Agent，
不需要这份提示词 —— 直接按 [README](README.md) 把技能装进去，
它们能自己读 `SKILL.md`、改文件、跑脚本。

这份提示词是给**只有对话框、不能操作你电脑**的场景准备的：
网页版豆包 / Kimi / 通义千问 / DeepSeek，或者你不想授权本机操作权限的时候。

好消息是模板已经是成品，AI 只需要改里面的文字，对模型能力要求不高。

## 怎么用（三步）

1. **上传** `templates/deck.html`（详情页）和 `templates/poster.html`（海报）
   到对话里。如果工具不支持传文件，就把文件内容整段粘进去
2. **复制下面整段提示词**，连同你的活动信息一起发给它
3. **把 AI 给你的完整 HTML 存成 `index.html`**，按
   `references/01-部署攻略.md` 上传到网上

> **提示**：如果 AI 的单次输出长度不够，让它**分段输出**，
> 或者告诉它「只给我需要修改的那些行，我自己替换」——
> 后一种方式更省事，也不容易出错。

---

## 提示词正文（从这里开始复制）

```
你是一位擅长中文排版的网页设计师。我上传了一个微信 H5 邀请函的成品模板，
请你把它改成我的活动信息。

【最重要的规则】
1. 只改文字内容，不要重写 CSS。那套排版（字阶、间距、配色、名单对齐）
   已经调过很多轮，从零生成大概率不如它。
2. 保持所有 class 名称和 HTML 结构不变。
3. 如果我给的信息比模板里的条目多或少，增删对应的 HTML 块即可，
   不要改变块的结构。

【一条硬约束】
每一屏的内容必须能在一屏内看完，不许要下拉。手机可视高度按 700px 算，
每屏内容不超过 660px。日程超过 7 讲就拆成两屏。
你改完之后请告诉我哪几屏内容偏多、可能需要压缩。

【删改注意】
如果你删掉了某个 HTML 元素，检查页面底部的 <script> 里有没有
document.getElementById 引用它。有的话必须一起删 ——
否则脚本会在那一行报错并整个中断，后面所有功能（背景音乐、复制地址）
都会静默失效，而且语法检查发现不了。

【我的活动信息】
- 主办／承办／执行：
- 活动全名：
- 副题：
- 一句话主张（8–16 字，写参与者能带走什么，不要写口号）：
- 时间：
- 地点：
- 详细地址（要精确到能被地图搜到）：
- 日程（每场：时间 / 讲者 / 题目）：
- 团队名单（可选）：
- 行动点（扫码进群 / 报名表单 / 电话 / 纯信息展示）：

【请你输出】
改好的完整 HTML。如果太长就分段给我，或者只给需要替换的片段并标明位置。
```

（复制到这里为止）

---

## 改完之后

### 必做的检查

在电脑浏览器里打开改好的 HTML，把窗口拖到大约手机的宽度（375px 左右），
逐屏看一遍：

- 每一屏都不用下拉就能看全
- 没有文字被右下角的音符按钮压住
- 中文标点是全角
- 人名、机构名、地址逐字核对（**同音字会导致导航到错的地方**）

浏览器控制台里粘贴 `scripts/check-layout.js` 可以自动检查屏高。

### 海报

`templates/poster.html` 同样只改文字。改完把里面的
`DETAIL_URL` 换成详情页的真实网址，然后：

- **有 Mac/Windows 电脑**：运行 `scripts/shot.sh` 自动出图
- **不想装环境**：用浏览器打开 poster.html，
  按 `F12` 打开开发者工具 → 手机模式 → 设成 1080×1920 → 截图
- **完全不想碰命令行**：把网页截长图也行，只是尺寸不够精确

### 背景音乐

把 mp3 文件命名为 `bgm.mp3` 放在 index.html 同一个目录。
没有这个文件时，音符按钮会自动隐藏，不会报错。

建议：单声道、64–96kbps、2–3 分钟，控制在 1.5MB 以内。
用免费授权的曲子（如 CC0 / CC BY），在页脚注明来源。

---

## 常见问题

**Q：AI 把 CSS 也改了，页面变丑了**
A：重新开一轮对话，明确说"只改 `<body>` 里的文字，`<style>` 一个字都不要动"。

**Q：AI 输出到一半断了**
A：让它「接着上次断的地方继续」，或者改用"只给修改片段"的方式。

**Q：改完打开是空白页**
A：多半是 HTML 标签没闭合。把文件发回给 AI，说"这个文件打开是空白，
请检查标签闭合"。

**Q：我不会用命令行，能完成吗？**
A：能。改文件用 AI，部署走 GitHub Pages 网页上传（全程点鼠标），
海报用浏览器开发者工具截图。整条链路都不需要命令行。
